#include <stdio.h>

int main() {
    float C, F;
    printf("Enter temperature in Celsius: ");
    scanf("%f", &C);
    F = (C * 9 / 5) + 32;
    printf("Temperature in Fahrenheit: %.2f\n", F);
    return 0;
}
ALGORITHM :

1. Takes input for temperature in Celsius (C).


2. Applies formula: F = (C × 9/5) + 32.


3. Displays the result in Fahrenheit (F).
